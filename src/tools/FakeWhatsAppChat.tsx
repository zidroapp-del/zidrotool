import { useRef, useState } from "react";
import { Download, MessageCircle, Plus, Trash2 } from "lucide-react";
import { useTranslation } from "react-i18next";
import { useToast } from "@/components/Toast";
interface Msg{id:number;side:"in"|"out";text:string;time:string}
export default function FakeWhatsAppChat(){const{t}=useTranslation();const{error}=useToast();const ref=useRef<HTMLDivElement>(null);const[name,setName]=useState("Alex");const[avatar,setAvatar]=useState("A");const[msgs,setMsgs]=useState<Msg[]>([{id:1,side:"in",text:"Hey! How are you?",time:"10:24"},{id:2,side:"out",text:"I'm good, thanks!",time:"10:25"}]);const[text,setText]=useState("");const[side,setSide]=useState<"in"|"out">("in");const add=()=>{if(!text.trim())return;setMsgs(m=>[...m,{id:Date.now(),side,text:text.trim(),time:new Date().toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}]);setText("")};const exportImage=async()=>{if(!ref.current){return}try{const node=ref.current;const canvas=document.createElement("canvas");const w=720;const h=Math.max(520,node.scrollHeight+40);canvas.width=w;canvas.height=h;const ctx=canvas.getContext("2d")!;ctx.fillStyle="#e5ddd5";ctx.fillRect(0,0,w,h);ctx.fillStyle="#075e54";ctx.fillRect(0,0,w,72);ctx.fillStyle="#fff";ctx.font="bold 22px sans-serif";ctx.fillText(name,70,44);ctx.fillStyle="#25d366";ctx.beginPath();ctx.arc(35,36,20,0,Math.PI*2);ctx.fill();ctx.fillStyle="#075e54";ctx.font="bold 18px sans-serif";ctx.fillText(avatar.slice(0,1).toUpperCase(),29,42);let y=105;ctx.font="18px sans-serif";for(const m of msgs){const width=Math.min(520,Math.max(180,ctx.measureText(m.text).width+60));const x=m.side==="out"?w-width-28:28;ctx.fillStyle=m.side==="out"?"#dcf8c6":"#fff";ctx.fillRect(x,y,width,Math.max(54,ctx.measureText(m.text).width>500?80:54));ctx.fillStyle="#222";ctx.fillText(m.text,x+18,y+29);ctx.fillStyle="#777";ctx.font="12px sans-serif";ctx.fillText(m.time,x+18,y+48);ctx.font="18px sans-serif";y+=78}const a=document.createElement("a");a.href=canvas.toDataURL("image/png");a.download="fictional-chat.png";a.click()}catch(e){console.error(e);error(t("tool.error"))}};return <div className="space-y-5"><div className="grid gap-5 lg:grid-cols-[1fr_360px]"><div className="space-y-4"><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-medium">{t("tool.whatsapp.name")}<input value={name} onChange={e=>setName(e.target.value)} className="input mt-2 w-full" /></label><label className="text-sm font-medium">{t("tool.whatsapp.avatar")}<input value={avatar} onChange={e=>setAvatar(e.target.value)} maxLength={2} className="input mt-2 w-full" /></label></div><div className="flex gap-2"><select value={side} onChange={e=>setSide(e.target.value as "in"|"out")} className="input w-32"><option value="in">{t("tool.whatsapp.incoming")}</option><option value="out">{t("tool.whatsapp.outgoing")}</option></select><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')add()}} placeholder={t("tool.whatsapp.messagePlaceholder")} className="input flex-1"/><button type="button" onClick={add} className="btn-primary"><Plus className="h-4 w-4"/></button></div><button type="button" onClick={()=>setMsgs([])} className="btn-secondary"><Trash2 className="h-4 w-4"/>{t("tool.whatsapp.clear")}</button></div><div ref={ref} className="rounded-2xl border border-ink-200 bg-[#e5ddd5] p-4 dark:border-ink-700"><div className="flex items-center gap-3 rounded-xl bg-[#075e54] p-3 text-white"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-[#25d366] font-bold text-[#075e54]">{avatar.slice(0,1).toUpperCase()||"A"}</div><div><p className="font-semibold">{name||"Contact"}</p><p className="text-xs opacity-75">{t("tool.whatsapp.fictional")}</p></div></div><div className="mt-4 min-h-80 space-y-2">{msgs.map(m=><div key={m.id} className={`flex ${m.side==='out'?'justify-end':'justify-start'}`}><div className={`max-w-[85%] rounded-xl px-3 py-2 text-sm shadow-sm ${m.side==='out'?'bg-[#dcf8c6]':'bg-white'}`}><p className="whitespace-pre-wrap break-words">{m.text}</p><p className="mt-1 text-right text-[10px] text-ink-500">{m.time}</p></div></div>)}</div></div></div><button type="button" onClick={exportImage} className="btn-primary w-full sm:w-auto"><Download className="h-4 w-4"/>{t("tool.whatsapp.export")}</button><p className="text-xs text-ink-400">{t("tool.whatsapp.disclaimer")}</p></div>}
