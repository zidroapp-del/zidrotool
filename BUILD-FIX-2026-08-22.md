# Build fix — 2026-08-22

Fixed two syntax errors introduced in the SEO utility expansion:

- Added the missing comma after the last finance tool entry in `src/data/catalog.ts` before the new SEO utility tools block.
- Added the missing comma after the PDF compressor translation object in `src/locales/en.ts` before the new SEO utility translations.

The reported `TS1005` / `Expected "}"` errors were caused by these missing commas.

## Verify locally

```bash
npm run typecheck
npm run build
```

The `npm audit` vulnerabilities and npm `allow-scripts` notices are dependency/security-policy items and are intentionally not auto-fixed here because `npm audit fix --force` can introduce breaking dependency changes. Review them separately after the build is green.
